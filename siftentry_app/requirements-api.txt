# SiftEntry FastAPI backend — production API dependencies only.
# The Streamlit pilot's extras live in requirements.txt (which includes this file).
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
pydantic>=2.10,<3
python-multipart>=0.0.20
httpx>=0.28,<1
pwdlib[argon2]>=0.3,<1
PyJWT>=2.10,<3
# PDF text + word-coordinate extraction (parser_service, evidence bboxes)
pymupdf
pypdf
